package model;

public enum Enum_SQLDataTypes {
		ENUM0,
		ENUM1,
		ENUM2,
		ENUM3,
		INTEGER,
		ENUM5,
		ENUM6,
		ENUM7,
		FLOAT,
		ENUM9,
		ENUM10,
		ENUM11,
		VARCHAR,
		ENUM13,
		ENUM14,
		ENUM15,
		ENUM16,
		ENUM17,
		ENUM18,
		ENUM19,
		ENUM20,
		ENUM21,
		ENUM22,
		ENUM23,
		ENUM24,
		ENUM25,
		ENUM26,
		ENUM27,
		ENUM28,
		ENUM29,
		ENUM30,
		ENUM31,
		ENUM32,
		ENUM33,
		ENUM34,
		ENUM35,
		ENUM36,
		ENUM37,
		ENUM38,
		ENUM39,
		ENUM40,
		ENUM41,
		ENUM42,
		ENUM43,
		ENUM44,
		ENUM45,
		ENUM46,
		ENUM47,
		ENUM48,
		ENUM49,
		ENUM50,
		ENUM51,
		ENUM52,
		ENUM53,
		ENUM54,
		ENUM55,
		ENUM56,
		ENUM57,
		ENUM58,
		ENUM59,
		ENUM60,
		ENUM61,
		ENUM62,
		ENUM63,
		ENUM64,
		ENUM65,
		ENUM66,
		ENUM67,
		ENUM68,
		ENUM69,
		ENUM70,
		ENUM71,
		ENUM72,
		ENUM73,
		ENUM74,
		ENUM75,
		ENUM76,
		ENUM77,
		ENUM78,
		ENUM79,
		ENUM80,
		ENUM81,
		ENUM82,
		ENUM83,
		ENUM84,
		ENUM85,
		ENUM86,
		ENUM87,
		ENUM88,
		ENUM89,
		ENUM90,
		ENUM91,
		ENUM92,
		DATE,
		ENUM94,
		ENUM95,
		ENUM96,
		ENUM97,
		ENUM98,
		ENUM99,
		ENUM100
		}